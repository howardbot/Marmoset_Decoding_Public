# Plotting Scripts

These scripts generate established sweep and publication figures. Generated
images belong under `Results/`, never beside the plotting code. The directory
was renamed from `figs/` so code and figure artifacts are unambiguous.
