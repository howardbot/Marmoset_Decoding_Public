# Curated Results

This public snapshot includes `current/` plus the additional historical-path
tables and figures cited directly by the current report. Large intermediate
tables, parameter sweeps, shards, backups, and generated QA artifacts are not
included.

New outputs should use lowercase snake_case, place figures and tables in
separate directories, and avoid ambiguous labels such as `final` or `latest`.
